apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: sourcers.example.digi.dev
spec:
  group: example.digi.dev
  names:
    kind: Sourcer
    listKind: SourcerList
    plural: sourcers
    singular: sourcer
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              meta:
                properties:
                  registry:
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
