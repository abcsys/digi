group: digi.dev
version: v1
kind: lake
pools:
  openapi:
    additionalProperties:
      properties:
        head:
          type: string
        last_updated:
          type: string
        size:
          type: number
      type: object
    type: object